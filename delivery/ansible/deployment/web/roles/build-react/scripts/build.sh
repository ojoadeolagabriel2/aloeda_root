#!/usr/bin/env bash

#####################################################################################################################################
# Build react app
#####################################################################################################################################

set +e

PROJECT_DIR="${3}"

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

# prep standalone package
(cd ${PROJECT_DIR} && npm run-script build:microapp)